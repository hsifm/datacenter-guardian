version: '3.8'

services:
  redfish-proxy:
    build: .
    container_name: redfish-proxy
    restart: unless-stopped
    ports:
      - "8443:8443"
    networks:
      - datacenter-net

networks:
  datacenter-net:
    driver: bridge
